# file: roles/base_osx/defaults/main.yml

---
base_osx:
  # common ---------------------------------------------------------------- {{{
  common:
    # taps --------------------------------------------------------- {{{
    taps:
      - homebrew/dupes
      - homebrew/science
      - homebrew/versions
      - nviennot/tmate
      - phinze/cask
    # /taps -------------------------------------------------------- }}}

    # packages ----------------------------------------------------- {{{
    packages:
      - ansible
      - aspell
      - bitlbee
      - brew-cask
      - chromedriver
      - cloc
      - contacts
      - coreutils
      - cowsay
      - csshx
      - ctags
      - curl-ca-bundle
      - ec2-api-tools
      - elasticsearch
      - elixir
      - erlang
      - exiftool
      - fdupes
      - figlet
      - fish
      - fortune
      - gcc49
      - geoip
      - gfortran
      - git
      - gnu-sed
      - go
      - graphviz
      - haskell-platform
      - htop-osx
      - imagemagick
      - irssi
      - john
      - jq
      - leiningen
      - libgit2
      - macvim
      - mariadb
      - mongodb
      - mpc
      - mpd
      - msmtp
      - mtr
      - mutt
      - ncdu
      - nginx
      - nmap
      - node
      - notmuch
      - offline-imap
      - postgresql
      - pypy
      - rabbitmq
      - reattach-to-user-namespace
      - rebar
      - rethinkdb
      - ruby
      - selenium-server-standalone
      - sqlite
      - task
      - the_silver_searcher
      - tmate
      - tmux
      - urlview
      - vim
      - vimpager
      - vnstat
      - watchman
      - wget
      - wireshark
      - xapian
      - zeromq
      - zsh
    # /packages ---------------------------------------------------- }}}

    # casks -------------------------------------------------------- {{{
    casks:
      - alfred
      - bartender
      - caffeine
      - charles
      - iterm2
      # - dropbox
      # - little-snitch
      # - size-up
      # - virtualbox
    # /casks ------------------------------------------------------- }}}

  # /common --------------------------------------------------------------- }}}

  # python_packages ------------------------------------------------------- {{{
  python_packages:
    # requirements ------------------------------------------------- {{{
    requirements:
      - Fabric
      - Flask
      - Jinja2
      - SQLAlchemy
      - Sphinx
      - Werkzeug
      - boto
      - bpython
      - dulwich
      - flake8
      - hieroglyph
      - httpie
      - mercurial
      - passlib
      - pip-tools
      - psdash
      - psutil
      - pythong
      - requests
      - see
      - six
      - uWSGI
      - virtualenv
      - virtualenvwrapper
    # /requirements ------------------------------------------------ }}}
  # /python_packages ------------------------------------------------------ }}}
